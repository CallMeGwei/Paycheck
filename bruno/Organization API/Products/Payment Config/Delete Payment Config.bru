meta {
  name: Delete Payment Config
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/orgs/{{org_id}}/projects/{{project_id}}/products/{{product_id}}/payment-config/{{payment_config_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{org_api_key}}
}

docs {
  Delete a payment configuration.
  Removes the payment provider integration for this product.
}
